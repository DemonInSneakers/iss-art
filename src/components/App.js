import React, { Component } from 'react';
import './App.css';
import {Tables} from './containers'

class App extends Component {
        render() {
        return (
            <Tables/>
        );
    }
}

export default App;
